package arao.jatc.controller.activities;

/**
 * Performs controller operations on reaction of UI events on the Splash screen
 */
public interface SplashActivityController extends ActivityController {

    void onLoginButtonClicked();
}
